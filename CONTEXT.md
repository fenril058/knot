# knot

Cosense（旧 Scrapbox）のデータ構造と記法に互換な、セルフホスト可能な wiki。
この用語集は、knot の中で意味を統一する必要がある語を定義する。

## ページとタイトル

**プロジェクト**:
複数のページを収める独立した wiki 空間。

**ページ**:
1 つのプロジェクトに属する、順序づけられた行の並び。

**タイトル行**:
ページのタイトルとして扱う先頭行。

**title_lc**:
大文字小文字、空白とアンダースコア、Unicode の表現揺れを吸収してタイトルを照合するための正規形。

**行**:
ページを構成し、本文が変わっても同一性を保つ単位。

## 書き込み

**web-first**:
ブラウザからの閲覧と編集を主要な利用経路とし、ローカルエディタなどを補助経路として扱う方針。

**server-authoritative 編集モデル**:
クライアントが基準にしたページ version と変更を送り、サーバが変更の採否と適用順を決める編集モデル。

**正規形式**:
ページ本文の変更をコミットログに記録するときの唯一の表現。
knot では行操作を正規形式とする。
_Avoid_: 正準形式

**行操作**:
行の挿入、更新、削除として表したページ本文の変更。
_Avoid_: 編集操作、パッチ

**コミット**:
1 つのページへ原子的に適用する行操作のまとまり。

**commitId**:
コミットを一意に識別し、同じコミットの再送を見分ける識別子。

**ページ version**:
サーバが受理したページ本文の版を識別する値。

**baseVersion**:
コミットを作成するときに基準としたページ version。

**リベース**:
version 競合後に、手元の変更を最新のページ本文へ適用し直して行操作を作り直すこと。
_Avoid_: マージ、コンフリクト解決

**収束**:
すべてのクライアントが最終的にサーバと同じページ状態へ到達すること。
競合した変更がすべて残ることまでは意味しない。

## ページから得る情報

**導出データ**:
ページの行を一次データとして、必要なら作り直せる情報。

**代表画像**:
ページを視覚的に代表する、本文中の最初の画像。

**テロメア**:
行ごとの編集の新しさを示す表示。

## リンク

**空リンク**:
まだ存在しないページを指すブラケットリンク。
_Avoid_: 赤リンク、デッドリンク

**逆リンク**:
対象のページを指している別のページからのリンク。
_Avoid_: バックリンク、被リンク

**1-hop リンク**:
対象のページが直接指す実在ページと、対象のページへの逆リンクの集合。

**2-hop リンク**:
対象のページとリンク先を共有し、自分自身と 1-hop リンクを除いたページの集合。

**関連ページ**:
1-hop リンクと 2-hop リンクを合わせたページ群。
