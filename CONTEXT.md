# knot

Cosense（旧 Scrapbox）のデータ構造と記法に互換な、セルフホスト可能な wiki。
Cosense のエクスポート JSON を無加工で取り込み、いつでも Cosense 互換 JSON で書き出せる「受け皿」であることが存在理由である。

収めるのは、knot で意味を固定しておかないと食い違う語だけである。
一般的な語（コミット、リベース）も、knot での意味が一般的な用法とずれるなら収める。
逆に、意味が自明で揺れようのない語は、このリポジトリで多用していても載せない。

## ページとタイトル

**プロジェクト**:
独立した wiki 空間。Cosense のプロジェクトに相当する。
1 つの knot インスタンスが複数を持つ。

**ページ**:
1 つのプロジェクトに属する、順序づけられた行の並び。

**タイトル行**:
ページの先頭行。Cosense と同じく、これがページのタイトルそのものである。
別フィールドとしてのタイトルは存在せず、先頭行が入れ替わればタイトル変更として扱う。

**title_lc**:
タイトルを Unicode NFC 正規化し、小文字化し、空白 (U+0020) を `_` に置換した文字列。
リンク解決と URL 解決はこの一致で行うので、`[Foo Bar]` と `[foo_bar]` は同じページを指す。
Cosense の titleLc の観察された挙動に合わせた knot 独自の定義であり、Cosense 内部実装との完全一致は保証しない。

**行**:
ページを構成する単位。行 ID を持ち、テキストが書き換わっても ID は保たれる。
競合解決の単位でもある（Cosense と同じ粒度）。

## 書き込み

**正準形式**:
ページ本文の変更をコミットログ上で表す唯一の表現。knot では行操作がそれにあたる。
実行経路の話ではない（インポートは行を直接書き、等価な操作列を記録する）。

**行操作**:
`insert` / `update` / `delete` の 3 種。ページ本文の変更の正準形式。
_Avoid_: 編集操作、パッチ

**コミット**:
`{commitId, baseVersion, ops[]}` を単位とするページ本文の変更。単一トランザクションで適用される。
ピン留めと閲覧記録は本文の変更ではないのでコミットにならない。

**commitId**:
コミットの ID であり、再送の冪等性キー。
API 経由のコミットではクライアントが発行し、サーバ内部で組み立てるコミット（削除、リネーム、リンク書き換え、インポート）ではサーバが発行する。
同じ `commitId` の再送は `ops_hash` で内容の同一性を照合し、一致すれば最初の version を返し、違えば拒否する。
リベースで操作列が変わったら新しい `commitId` を発行する。

**ページ version**:
コミットが 1 つ適用されるたびに 1 増える整数。ページ本文の状態を一意に指す。

**baseVersion**:
コミットが「どの本文を見て作られたか」を示すページ version。
現在の値と食い違えば 409 を返す。
一致しても、結果のタイトルが既存ページと衝突すれば 409 になる。

**リベース**:
409 を受けたクライアントが、3 つの行スナップショット（最後に同期した状態、手元の状態、最新状態）から操作列を作り直すこと。
コミット列を必要としない純関数である。
_Avoid_: マージ、コンフリクト解決

**収束**:
すべてのクライアントが最終的にサーバと同じページ状態へ到達すること。
競合した双方の変更が両方残ることまでは意味しない。

## 導出データ

**導出データ**:
`lines` から再構築できる二次データ。`links`、`pages_fts`、`pages.image` が該当する。
`knot reindex` で作り直せる状態を保つ。

**代表画像**:
ページ内の最初の画像 URL（なければ空）。
ページ一覧のカード、読み取り API の image フィールド、リンク先のプレビューに使う。

**テロメア**:
行ごとの編集の新しさの可視化。
未読判定は時刻ではなく version の比較（`lines.updated_version > last_seen_version`）で行う。
行更新と閲覧が同じ Unix 秒に起きても判定が曖昧にならないためである。

## リンク

**空リンク**:
まだ存在しないページを指すブラケットリンク。
_Avoid_: 赤リンク、デッドリンク（表示色は変わりうるので、色を語に含めない）

**逆リンク**:
そのページを指している他のページ。
`[X.icon]` によるアイコン参照もリンクとして記録されるので、「逆リンクの有無」の判定にはアイコン参照だけのページも含まれる。
_Avoid_: バックリンク、被リンク

**1-hop リンク**:
そのページの前方リンク先として実在するページと、逆リンクの和。

**2-hop リンク**:
そのページの前方リンク先へ張っている他のページ。自分自身と 1-hop は除く。
リンク先が空リンクでも成立するので、まだ存在しないページを介してページ同士がつながる。
これが Cosense の自己組織化の中核である。

**関連ページ**:
1-hop リンクと 2-hop リンクを合わせた、ページ下部に並ぶページ群。
